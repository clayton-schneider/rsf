---
export interface Props {
  text: string;
  className?: string;
}

const { text, className } = Astro.props;
---

<p class={className}>
  {text.split("\n").map((text, i) => (i ? [<br />, text] : text))}
</p>
